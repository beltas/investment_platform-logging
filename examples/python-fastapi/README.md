# Python FastAPI Example

Example FastAPI application using agora-log.

## Setup

```bash
pip install agora-log[fastapi] fastapi uvicorn
```

## Run

```bash
uvicorn main:app --reload
```

## Files

- `main.py` - FastAPI application with logging middleware
- `logging.yaml` - Example logging configuration
